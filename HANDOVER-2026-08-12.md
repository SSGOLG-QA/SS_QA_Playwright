# 핸드오버 — 코스관리 코스현황·정보관리 전 화면 심화 + 버튼 커버리지 감사 (2026-08-12)

> durable 상세는 메모리 `course-plan-v1.1.md`에 반영됨. 이 문서는 세션 인수인계 요약.
> 대상: **코스관리**(`course-mng-td.smartscore.kr`, 클럽 킹즈락). 경기관제(td17)와 별개 프로젝트.
> 이전 핸드오버(P4 자동화 확장, 2026-08-05, Admin/td17)는 `HANDOVER.md` 참조.

## 1. 이번 세션 작업 — 코스 모니터→식생→시각화면→정보관리 심화 + 감사 공용화
사용자 요청 흐름(순차): 이슈 인라인패널 → 점검 → 관심·인력 → 식생 분석 → 코스현황 잔여 4화면 → 정보 관리 11화면 → **버튼 커버리지 감사(조용한 누락 방지) 공용화·전 스펙 적용**.

| # | 작업 | 신규 spec (npm run) | 결과 |
|---|------|--------------------|------|
| 1 | 코스 모니터 **이슈** 탭 심화 | `course:issue` | PASS 11 |
| 2 | 코스 모니터 **점검** 탭 심화 | `course:inspection` | PASS 10 |
| 3 | 코스 모니터 **관심·인력** 탭 심화 | `course:montab` (`$env:MON_TAB=관심\|인력`) | 관심 9 / 인력 4 |
| 4 | **식생 분석** 심화(레이어·색상범위 드롭 포함) | `course:vegetation` | PASS 11 |
| 5 | 코스현황 잔여 **4화면**(그린·3D·영역·드론) | `course:visual` | PASS 18 |
| 6 | **정보 관리 11화면** 균일 배터리 12종 | `course:info-deep` | PASS 56 |
| 7 | **버튼 커버리지 감사 공용 헬퍼** | `lib/course/coverageAudit.ts` | 6 스펙 적용 |

전부 `npm run typecheck`(tsconfig.typecheck.json) 0 에러 + 라이브 검증(각 spec `course:auth` 직후 1런). **FAIL 0**.

## 2. 핵심 성과 — 버튼 커버리지 감사 (QA 피드백 대응)
**지적**: 고정 배터리는 *없는* 컨트롤(필터/내보내기/검색)엔 SKIP 케이스를 만들지만, *있으나 배터리에 없는* 버튼(수정·저장·초기화 등)은 **케이스 자체가 없어 조용히 누락**.

**해결**: `lib/course/coverageAudit.ts`의 **`auditButtonCoverage(admin, P, tcRef, tcId, opts?)`** — 각 화면 검사 끝 1회 호출.
- 화면 표시 버튼(`button:visible`) 전수 열거 → **handled**(스위트가 상호작용) / **파괴제외**(저장·삭제=의도적 미클릭) / **프레임워크**(help·페이지네이션·모달) / **미처리** 분류.
- **미처리 > 0 → `diff`로 명시화**(전용 검사 추가 대상). 신규 버튼 생기면 자동 surface.
- 도메인 기본 어휘 내장 + `opts.extraHandled`로 화면별 확장(예: 이슈/점검 `상·중·하`, 그린 `좌·우`, 3D `지점/거리/높이/각도/넓이`).
- **6개 심화 스펙 전체 적용·검증: 전 화면 BTNCOV PASS·미처리 0.**

부수로 **코스 기본 정보 [수정]** 등 존재-미테스트 버튼에 `checkEdit`(편집모드 진입→취소, 비파괴)·`checkReset`·`checkDatePreset` 추가.

## 3. 산출물
- **신규 spec 6종** + **공용 헬퍼 1종**(위 표). 모두 `Course/`·`lib/course/`.
- **통합 리포트**: `reports/course-integrated-report.html` — **21 spec행 / PASS 310 / FAIL 0 / SKIP 108 / diff 8**. "버튼 커버리지 감사" 섹션 포함.
- `package.json`: `course:issue`·`course:inspection`·`course:montab`·`course:vegetation`·`course:visual`·`course:info-deep` 스크립트 추가.
- 진단 덤프(scratchpad) 및 인라인 진단 코드는 셀렉터 확정 후 전부 정리(감지 로직만 유지).

## 4. 실행 방법 & ⚠️ 세션 제약
- **세션 "재로그인 1회당 1런만 생존"**(공유 QA 계정). → 각 spec 실행 전 **`npm run course:auth`(수동 헤디드 로그인) 필수**, 1런만 healthy.
  ```bash
  npm run course:auth        # 수동 로그인 → auth/.auth/course.json
  npm run course:info-deep   # (또는 course:issue / course:vegetation / …)
  ```
- montab은 env로 탭 선택: `$env:MON_TAB="인력"; npm run course:montab`.
- 통합 리포트는 **개별 fresh-login 런 결과를 집계**(한 번에 다 돌리면 2번째부터 세션 저하 = 가짜 FAIL). `openCourseAdmin`이 degraded 세션 fail-fast.
- 비파괴 원칙: 저장/삭제 클릭 안 함(노출·모달 열기→취소만), 토글/필터는 원복.

## 5. 화면 구조 교훈 (인수자 참고 — 재발 방지)
- **화면마다 표현이 판이** → 첫 런은 **discovery 덤프**(버튼/select/슬라이더/캔버스/panel/marker)로 실제 구조 파악 후 검사 재설계(대개 2런 수렴).
- **레이어·색상범위 = native `<select>`**(vue-select 아님): 레이어=식생지수 9종(ndvi/NDRE/CIR/RGB…), 색상범위=`sp-floor-sel`(투명/파랑/회색). `pickDropByLabel`(라벨→xpath ancestor 행→selectOption).
- **행 액션(보기/수정)은 hover 노출형** — `querySelectorAll`엔 있으나 `isVisible=false` → 행 hover 후 클릭(force 폴백). 홀별·운영은 `button:visible=0`(전부 hover) → 감사 skip.
- **[크게 보기]**: 텍스트에 공백(`크게 보기`) + 상세 패널 자체 스크롤 하단 → `scrollIntoViewIfNeeded` + 확대 모달 `.modal-group.task-big-map-modal`.
- **인라인 패널 토글 = `button.panel-button`**: CSS transform 슬라이드라 width/isVisible 불변 → 버튼 클래스/`.slide-panel` left·width 변화로 판정. 관심 탭엔 검색 버튼 없음 → 앵커는 `.slide-panel`.
- **인력 탭 = 지도 마커 전용 뷰**(카드·패널 없음) → 인라인 심화 미적용, 마커 클릭으로 대체 + diff.
- 코스 datepicker = **fill 입력**(경기관제 달력전용과 상이). `→` 접미 버튼(배수/비교 분석)=네비게이션(toolToggle 부적합).

## 6. 버튼 감사 전역 확대 (2026-08-12 추가 완료)
"모든 케이스 생성 시 적용" 후속 → **전역 감사 스윕**으로 확대(스펙별 wiring보다 효율적).
- **`course:btn-audit`**(`course-button-audit-sweep.spec.ts`) — COURSE_IA **46화면 한 로그인 순회** 감사. 최초 스윕이 존재-미테스트 버튼 갭 맵 surface.
- **대응**: ① `coverageAudit.ts` 어휘 정제(정렬 초기화·날짜프리셋·코스 영역/구역 필터칩=handled, 저장하기=파괴제외) ② 스윕 `probeOpenCancel` — 등록/설정/업로드 버튼을 **직접 모달 open→cancel 테스트**(비파괴). 결과 **전 46화면 미처리 0**, 스윕 PASS 49/FAIL 0/SKIP 14.
- 실행: `npm run course:auth` → `npm run course:btn-audit`(~2.7분).

## 7-a. UI 구조 드리프트 baseline (2026-08-12 추가 완료)
이번 세션 대규모 커버리지의 **구현 변경 자가검증** 레이어.
- **`course:ui-baseline`**(`course-ui-baseline.spec.ts`, Admin ui-baseline 이식) — COURSE_IA 전 화면 구조 chrome(컬럼헤더·버튼·탭·안내문구) 스캔 → committed baseline 대조. **제거=FAIL(회귀)·추가=diff·화면소실=FAIL·신규화면=diff**.
- baseline **캡처 완료**: `baselines/course-ui-structure.course-mng-td.json`(44화면·헤더 277·버튼 142·탭 13·안내문구 95). diff 모드 검증 **노이즈 0**.
- 실행: `$env:UI_BASELINE="capture"; npm run course:ui-baseline`(갱신) / `npm run course:ui-baseline`(검출, ~2.5분).
- **버튼 감사와 상보**: 감사=버튼 분류·테스트 / baseline=구조 제거 회귀 검출.

## 7-b. 미착수 화면 갭 채우기 + 커버리지 (2026-08-12 추가 완료)
- **공용 배터리 추출** `lib/course/screenBattery.ts`(`runScreenBattery` 12종) — info-deep 배터리를 재사용 모듈로. 신규 데이터 화면은 이 한 줄로 균일 검증.
- **`course:gap`**(`course-gap-screens.spec.ts`) — 커버리지 0%/저커버 8화면(인력 근태·투입 / 사진 정보별·위치별 / 시설·장비·자재 총괄 / 자재 수불 품목별)에 배터리 적용. healthy **PASS 34/FAIL 0/SKIP 62**(low-chrome 화면이라 미해당 SKIP 다수 정당).
- **커버리지 56%→80%**(`course:coverage` 오프라인): 여정 56→63(검증분 등재)→72(`course:gap` 갭 채우기)→80(`course:controls` 비데이터 컨트롤). 테스트 대상 217·검증 174(80%)·L2 98(45%)·미커버 43.
- **`course:controls`**(`course-controls-fill.spec.ts`) — 시드 불필요 비데이터 컨트롤(근태 status 토글·비용 필터칩·적용/검색) 17 PASS. ⚠ 교훈: `getByRole().first().or(getByText().first())`는 같은 요소 role+text 2매치로 isVisible strict throw → `.or()` 전체에 `.first()` 적용해야. 필터바가 `.contents` 밖일 수 있어 admin 페이지 전역 검색.
- 잔여 미커버 43: 연도/대분류 커스텀 드롭·데이터 의존 보기(시드 필요)·맵 화면(DOM한계)·Home 액션(홈 미진입). 대부분 저가치.
- ⚠ **매니페스트 등재 필수**: `course:coverage`는 "등재된 라이브 PASS만" 집계 → 신규 검증 후 매니페스트에 `{label,kind,level,tcId}` 추가해야 수치 반영(인벤토리 라벨과 정규화 일치). 부풀리기 방지 정책.

## 7-c. 예산·비용 정합성 검증기 + 동적 write-path (2026-08-12 추가 완료)
- **`course:budget-verify`**(`course-budget-cost-verify.spec.ts` + `lib/course/domain/budgetCost.ts`) — 현재 시스템 데이터 수집(비파괴)→교차/내부/원천 불변식→**탭형 HTML**(`reports/course-budget-cost-verify.html`). 6탭: 실행방법/연관성맵/Summary/비용상세/예산상세/원천값검증. **PASS 56/0**.
  - 교차: 비용 총비용 4축 일치(집계=분류별=위치별=기간별), 관리비유형별 집계=Σ분류별.
  - 원천: 장비 시간당비용=매입가÷(내용연수×연간운용), 자재 단위당원가=총매입가÷재고, 원천 임률→비용 인건비 반영(568,182→568,181).
  - ⚠ 핵심 파싱: **rowspan 캡처→`gridOf` 격자 재구성**(대/중분류 채움), 실적 자재비 **4레벨 품명** breadcrumb 분리, 월값 원본 우측앵커. 킹즈락 test junk는 극단치(비율>5×)를 '확인필요'로 분리해 가짜 FAIL 방지.
- **`course:writepath`**(`course-budget-cost-writepath.spec.ts`, **옵트인 파괴**) — 예산 상세 값 +Δ 저장→상세 소계·예산 총괄 rollup 반영 확인→**원본 정확 복원**(3중 가드+finally). **PASS 4/0**. 실행: `$env:ALLOW_DESTRUCTIVE="1"; npm run course:auth` → `$env:ALLOW_DESTRUCTIVE="1"; npm run course:writepath`.
  - 편집모드: 분류 탭 선택→[수정]→행별 [적요+월12 input], 저장=[저장]→변경저장?확인→저장되었습니다확인.

## 7. 다음 후보 (미착수)
- **등록/설정 모달 내부 심화** — 현재 open→cancel(노출)까지. 모달 필드 입력·유효성·계산 정합성은 write-path(3중 가드)로 확장 가능.
- **엑셀 업로드·그룹 추가**(스윕 SKIP 3건): 클릭됐으나 표준 모달 미확정(filechooser/인라인 추정) → 개별 정밀 대응.
- **HL(홀별) [보기] 비표준 상세**(인라인 확장 추정, 모달 아님) 정밀 대응 — 현재 skip.
- **정보 관리 값 정합성**(기존 `course:info`)과 상호작용(`course:info-deep`) 통합 여부 검토.

## 8. 커밋 상태
- ⚠️ **미커밋** — 이번 세션 산출물은 워킹트리에 있음. 인수자가 리뷰 후 커밋 요망.
  - 신규 spec: `Course/course-{issue,inspection,montab,vegetation,visual,info}-deep.spec.ts`, `Course/course-button-audit-sweep.spec.ts`, `Course/course-ui-baseline.spec.ts`, `Course/course-gap-screens.spec.ts`, `Course/course-controls-fill.spec.ts`
  - 신규 spec(예산·비용): `Course/course-budget-cost-verify.spec.ts`, `Course/course-budget-cost-writepath.spec.ts`, `Course/course-controls-fill.spec.ts`, `Course/course-gap-screens.spec.ts`
  - 신규 lib: `lib/course/coverageAudit.ts`, `lib/course/screenBattery.ts`, `lib/course/domain/budgetCost.ts`
  - 신규 baseline: `baselines/course-ui-structure.course-mng-td.json`(44화면 UI 구조)
  - 갱신 baseline: `baselines/course-coverage-manifest.json`(커버리지 63→72% 반영)
  - 리포트: `reports/course-integrated-report.html`
  - 수정: `package.json`(course:issue/inspection/montab/vegetation/visual/info-deep/btn-audit/ui-baseline 스크립트)
  - 감사 적용 wiring: 심화 6종 spec에 `auditButtonCoverage` import+호출
- 커밋 전 `npm run typecheck`(tsconfig.typecheck.json) 확인(`--list`는 미임포트 못 잡음).
