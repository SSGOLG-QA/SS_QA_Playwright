# 인수인계 — 코스관리(Course Management) 자동화 확장 세션 (2026-08-14)

> 대상: `https://course-mng-td.smartscore.kr` 코스관리 어드민, 클럽 킹즈락(test 환경)
> 이전 인수인계: `HANDOVER-2026-08-12.md`. 상세 맥락은 memory `course-plan-v1.1.md`.

---

## 0. 이 세션 한 줄 요약
HOME·관제·계획·일보·그린분석·드론·3D·코스영역 등 **시각/특화 화면 심화** + **작업별 비용 전면 커버** + **예산/비용 정합성 심화(크로스이어 배분·이중계상 규명)**. 가장 큰 난관이던 **range datepicker 입력을 영상 프레임 분석으로 해결**.

---

## 1. 세션 제약 (필독 — 실행 전 숙지)
- **재로그인 1회당 신뢰 실행 1회.** 공유 QA 계정. 매 spec 실행 전 `npm run course:auth`(헤디드 수동 로그인) 필요. 2번째 실행부터 세션 저하(데이터 미로드·화면 SKIP).
- **저하 세션 징후**: 데이터 화면이 갑자기 전부 SKIP, 첫 화면만 PASS, `/login` 리다이렉트. 스펙 회귀 아님 → 재인증.
- **course:auth 헤디드 브라우저 정리**: `npm run course:kill`(ms-playwright Chromium만 종료, 사용자 Chrome 무해). `npm run course:auth-clean`=auth 후 자동 kill.
- **긴 스펙은 백그라운드로**: 4화면+ 스펙은 5~8분 → `run_in_background`로 실행.

---

## 2. 이번 세션 산출물 (신규 spec + npm script)

| npm script | 파일 | 결과 | 내용 |
|---|---|---|---|
| `course:home-verify` | `Course/course-home-verify.spec.ts` | PASS 12/0/3 | HOME 3탭(관리목표·현황/작업/비용) 정합성. 목표등급=목표설정모달·작업지시=작업지시목록·비용탭=예산실적 항등 |
| `course:gap2` | `Course/course-gap2-screens.spec.ts` | PASS 8/0/64 | 미등록 6화면(정보2·작업계획/일보·장비/시설관제) 배터리 스윕 |
| `course:mpd` | `Course/course-monitor-plan-daily-deep.spec.ts` | PASS 12/0/2 | 관제·계획·일보 심화. 데이터 먼저·지도 나중 순서(세션수명). SKIP2=세션 가장자리 |
| `course:uncov` | `Course/course-uncovered-fill.spec.ts` | PASS 6/0/0 | 미커버 가치항목(Home 3버튼·모니터 인력탭·거래처 초기화·권한 토글). 커버리지 80→83% |
| `course:drawzone` | `Course/course-drawzone-deep.spec.ts` | PASS 6/0/0 | 코스영역설정. **Leaflet 폴리곤 선택→정보모달(dispatch 기법)** |
| `course:green` | `Course/course-green-deep.spec.ts` | PASS 3/0/4 | 그린분석. 코스탭·그린정보팝업·미등록홀토스트 PASS. 6탭전환/더보기/제목/보기 미해결 |
| `course:costtask` | `Course/course-costtask-deep.spec.ts` | **PASS 13/0/0** | 작업별비용 전면. 기간(1년)·검색·분류·정렬6컬럼·계산식·작업명팝업·비용상세 |
| `course:crossyear` | `Course/course-crossyear-alloc.spec.ts` | PASS 3/1 | 크로스이어 배분 실증 = **모델 C(겹침 전액, 이중계상)** |
| `course:excl` | `Course/course-costtask-excl.spec.ts` | PASS 4/0/0 | 전년도시작 제외 계산 = 순수 당해시작 1,862,165 = 연도화면 일치 |
| `course:kill` | `scripts/kill-course-browser.ps1` | — | Playwright Chromium만 안전 종료 |

수정: `course-visual-deep.spec.ts`(드론 항목액션 5종·3D 측정/Orbit), `course-monitor-deep.spec.ts`(MON-POLY-SELECT), `course-budget-cost-verify.spec.ts`(1년 확장+전년도시작 제외), `lib/course/courseHelpers.ts`(setCourseDateRange/setCourseOneYear 신규).

---

## 3. ★ 핵심 기술 돌파구 (재사용 필수)

### 3-1. range datepicker 입력 (최대 난관, 15런+ 소요, 영상으로 해결)
`.datepicker-input` 2개 range picker. **nav 컨트롤(prev/next 버튼·년월 헤더) 클릭·키보드·JS세터 전부 무반응, 날짜 셀 클릭·직접 타이핑만 됨.**
- **정답(`setCourseDateRange` in courseHelpers)**: 입력이 대시를 자동 삽입 → **대시 없이 숫자 8자리만 타이핑**(`"20250101"`, `iso.replace(/[^0-9]/g,'')`). **대시 포함 타이핑·`fill('')`은 자동포맷과 충돌해 값이 깨져 사라짐**(시작 비어버린 근본원인).
- 방식: 시작 클릭→Ctrl+a→pressSequentially(숫자8)→종료 클릭(=시작 blur커밋)→Ctrl+a→숫자8→적용. **Enter/Tab/Escape 금지**(Escape는 값 되돌림).
- ⚠ **순차 재설정 시 세션 저하**(2~3번째 조회부터 재진입해도 실패) → 한 세션당 신뢰 조회 1회, 중요 쿼리를 첫 순서로.
- **영상 프레임 확인법**: `pip install opencv-python-headless` → cv2로 mp4 균등 프레임 추출 → 사용자 수동조작 육안 확인(대시 없이 숫자만 침을 발견). 막힐 때 결정적.
- 단일 날짜는 `pickCourseDate`(fill) OK, **범위는 반드시 setCourseDateRange**.

### 3-2. Leaflet 폴리곤 선택 (dispatch 기법)
좌표 클릭·boundingBox 불안정. **`.leaflet-interactive`(또는 `.leaflet-overlay-pane path`)에 네이티브 MouseEvent(mousedown/mouseup/click) `dispatchEvent`(bubbles:true)** → 좌표 히트테스트 우회. 검증 2화면(코스영역설정 정보모달, 코스모니터 하이라이트). ⚠ 맵마다 렌더 상이(구조 진단→넓은 셀렉터→충분한 렌더 대기). Canvas 렌더면 개별 DOM 부재로 dispatch 불가.

### 3-3. 아이콘 액션(시맨틱 클래스 없음) 탐색
드론 [코스모니터] 아이콘 클래스 = **`wh-24`**(의미 없는 유틸 클래스). 텍스트·타이틀·클래스 탐색 전부 실패 → **알려진 이웃 아이콘 기준 상대 위치**([3D]=`ico-3d`의 직전 형제)로 찾음. 스크린샷 순서 정보 활용. 항목 액션은 `<button>` 아닌 아이콘(`ico-edit`/`ico-delete`/`ico-3d`) — DRN-BTNCOV는 `<button>` 1종만 감지.

### 3-4. 팝업 비동기 로드 캡처 (그린 정보 팝업)
① **`killAlarms`가 팝업 [확인]을 오클릭해 닫음** → 팝업 열린 동안 killAlarms 금지. ② 팝업 제목 먼저·내용(탭/테이블) 비동기 로드 → 탭 텍스트 렌더까지 폴링. ③ 그린정보 포함 modal-group 여러 개(껍데기+실제) → **textContent 최장 modal 선택**.

---

## 4. ★ QA 공유 대상 발견 (제품 이슈 가능성)

### 4-1. 크로스이어 작업 원가 이중계상 (중요)
- **작업별 비용**(날짜 필터)은 크로스이어 작업(예 W-00058, 2025-09-01~2026-01-22, 7.5M)을 **겹치는 모든 기간에 전액 노출**(모델 C). 2025구간만 조회=7.5M, 2026구간만=7.5M.
- **연도필터 화면**(비용집계/분류별/위치별/기간별)은 **시작연도 기준** 계상.
- → **연도별 합산 시 크로스이어 작업 이중계상**(2025뷰+2026뷰=15M, 실제 7.5M).
- **실증**: `course:crossyear`(배분 모델), `course:excl`(제외 계산). **작업별에서 전년도시작 작업 제외 시 연도화면과 정확히 일치(1,862,165)** — budget-verify에 반영해 ★다축 일치 PASS.

### 4-2. datepicker nav 자동화 무반응
range datepicker의 월/년 네비게이션이 합성 이벤트·키보드에 무반응(실브라우저 수동만). E2E 자동화 저해. 대안=기본 조회기간 확대 or 프리셋 버튼.

### 4-3. 문구/구조 관찰
그린 이벤트 안내문구 오타 의심 등은 기존 memory 참조.

---

## 5. 미완/보류 항목
- **그린 분석 6탭 전환/더보기/제목/보기**(course:green): 팝업 **내부 탭 클릭**이 완강(data-attr 태깅해도 Vue 리렌더 소실 추정). 더보기/제목/보기는 데이터 의존(populated 탭 필요). 팝업 탭 leaf DOM 정밀 프로브 필요.
- **관제·계획·일보 SKIP 2**(course:mpd): 세션 수명 가장자리(마지막 화면). CI 전용계정이면 완주 가능.
- **3D 측정값**: 포인트 선택 상호작용은 검증되나 측정 수치값은 WebGL 캔버스 오버레이(DOM 미노출) — 시각 회귀 영역.
- **budget-verify 정보성 체크 수정**: '1년 적용 현황' 체크를 정보성(ok:true)으로 바꿈(직전) — 다음 실행 시 58/0 확인 권장.

---

## 6. 재개 시 워크플로
```bash
npm run course:kill          # 혹시 남은 Playwright 브라우저 정리
npm run course:auth          # 수동 로그인 → "저장 완료"
npm run course:kill          # auth 헤디드 고아 정리
npm run course:<spec>        # 테스트 1개 (긴 건 백그라운드)
```
- 커버리지: `npm run course:coverage`(오프라인, 매니페스트 기반 현재 83%). 신규 검증은 `baselines/course-coverage-manifest.json`에 live-PASS 등재 필요(anti-inflation).
- 통합 리포트: `reports/course-integrated-report.html`.
- 리포트 actual 값 읽기: exceljs로 시트 순회(컬럼 인덱스는 리포트별 상이, 셀 스캔 권장).

---

## 7. 다음 후보 (우선순위)
1. **QA 리포트**: 크로스이어 이중계상 + datepicker nav 무반응 → 제품팀 공유(Jira/Confluence).
2. **그린 분석 팝업 탭**: leaf DOM 정밀 프로브 후 6탭/더보기/제목/보기 재도전.
3. **budget-verify 58/0 확인** + 다른 비용 뷰(분류별/위치별)에도 전년도시작 제외 적용 검토.
4. **정합성 검증 연도별 확장**: 2025·2026 각각 다축 일치(단, 크로스이어 이중계상 고려).
