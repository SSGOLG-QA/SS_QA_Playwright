# HANDOVER 2026-08-18 — 그린팝업·QA리포트·스코어출력·정합성·보류규명

> SMARTSCORE 코스관리(course-mng-td) + 경기관제 어드민(td17) UI 검증 자동화. 이 문서는 다음 세션이 맥락을 이어받기 위한 안내서.

## 0. 한 줄 요약
인수인계(2026-08-14) **다음 후보 4건 전부 완료** + **스코어출력 신규화면 학습** + **보류 2항목(관제·계획·일보 SKIP 2 / 3D 측정값) 근본원인 규명·처리**. 반복 관통 교훈 = **팝업/지도 내부 상호작용은 Playwright 로케이터(리렌더 스테일) 대신 in-page 네이티브 dispatch + getMd 재선택**, **무거운 지도 2개는 한 런에 못 담음(격리)**, **rect 가시성(offsetParent 금지)**.

---

## 1. 완료 항목 (이번 세션)

| # | 작업 | 결과 | 실행 |
|---|------|------|------|
| A | budget-verify 리포트 개선 | ④Report탭·구분별표·연관성맵 원천노드·원천값↔비용비교·세션만료가드 | `course:budget-verify` |
| B | **그린 분석 팝업 탭**(인수 #2) | 6탭·더보기·제목·보기 **전부 PASS** | `course:green` 5/0/2 + `course:green-detail` 2/0/0 |
| C | **QA 리포트**(인수 #1) 크로스이어 이중계상 | HTML 아티팩트 발행 | `reports/qa-crossyear-cost-report.html` |
| D | **스코어 출력 설정** 신규화면 학습(td17) | 기획서→프로브→suite→spec **PASS 10/0/0** | `Admin/score-output.spec.ts` |
| E | budget-verify 58/0 + 전년도시작 제외 검토(인수 #3) | 58/0 확정 · 분류별/위치별 **제외 불필요** 확정 | `course:budget-verify` |
| F | 정합성 **연도별 확장**(인수 #4) | 기간별 연도별 다축(2024/2025/2026) 추가 → **61/0** | `course:budget-verify` |
| G | 보류 **FCMON-ENTER** 규명·해결 | SPA 제약 확정 → **격리 스펙 PASS** | `course:facility-monitor` 4/0/2 |
| H | 보류 **FILTER 미노출** 규명 | 장비=렌더레이스/존재 · 시설=필터부재(정당) | `course:filter-probe` |
| I | 보류 **3D 측정값** 시각회귀 | 스캐폴드 작성 + 스모크로 **헤드리스 불가·GPU 필요** 확정 | `course:3d-visual` |

### 신규 파일
- `Course/course-green-probe.spec.ts`(`course:green-probe`) · `Course/course-green-detail.spec.ts`(`course:green-detail`) · `lib/course/greenPopup.ts`
- `Admin/_probe-score-output.spec.ts` · `Admin/score-output.spec.ts` · `locators/admin-score-output.md` · `lib/suites.ts` `runScoreOutput`
- `reports/qa-crossyear-cost-report.html`(Artifact: claude.ai/code/artifact/ab8ccf01-…)
- `Course/course-facility-monitor.spec.ts`(`course:facility-monitor`) · `Course/course-filter-probe.spec.ts`(`course:filter-probe`) · `Course/course-3d-visual.spec.ts`(`course:3d-visual`)
- 수정: `course-green-deep.spec.ts` · `course-monitor-plan-daily-deep.spec.ts` · `course-budget-cost-verify.spec.ts`

---

## 2. ★ 핵심 교훈 (재사용)

### 2-1. 팝업/지도 내부 상호작용 = in-page 네이티브 dispatch
- **증상**: Playwright 로케이터가 Vue 리렌더로 스테일(`.last()`가 detached/wrong 해소). 그린 6탭·행버튼·제목 클릭이 완강.
- **해결**: `admin.evaluate`로 매번 **getMd 재선택**(rect 가시성 + 최장 textContent + 텍스트필터) 후 대상에 **pointer/mouse/click 네이티브 dispatch**. Playwright 로케이터 제거. (drawzone Leaflet 돌파와 동일)
- 전환 판정: **텍스트 해시 아님** → 탭은 `.tab-group .active` 클래스, 상세는 넓은 감지(url·`[class*=modal]` 오버레이 개수·body len 증가·키워드). **감지 전 killAlarms 금지**(상세/서브모달 [확인] 오클릭 닫힘).

### 2-2. offsetParent 함정
- `offsetParent`는 **position:fixed 모달서 null** → 가시성 판정에 쓰면 모달 소실 오판. **전부 `getBoundingClientRect().width/height>1`**.

### 2-3. 무거운 지도 화면은 격리 (SPA 제약)
- 지도(Leaflet) 화면 방문 후 **다음 지도-부모 SNB 아코디언 렌더 불능** + **SPA 딥링크 미지원**(`goto('/facility/monitor')`→`/index.html`) → 한 런에서 **두 번째 지도 화면 도달 불가**.
- **해결 = 격리 스펙**: 신선 세션에서 해당 화면을 **첫 지도 화면**으로 직행(첫 지도는 PASS). green-detail·facility-monitor 동일 패턴.

### 2-4. 세션 성격 정정 (사용자 확인)
- **계정 공유 아님 = CI 단독.** '1런/로그인'은 **세션 TTL/단발 토큰**(런 사이 만료)이지 동시 로그아웃 아님. **한 런 안에선 12+ 화면 견딤**(budget-verify 12화면 61/0 방증). → 런 내 SKIP은 세션 문제 아님(SPA/렌더/데이터).

### 2-5. 전년도시작 제외 = 작업별에만
- 크로스이어 작업은 **작업별(날짜필터)** 에서 겹치는 각 연도 전액 노출→연도합산 이중계상. **집계·분류별·위치별·기간별은 시작연도 기준**이라 이미 정합(교차검증 통과가 증거) → **추가 제외 불필요**(이중 제외=과소계상).

---

## 3. QA 공유 대상 (제품 이슈)

### 3-1. 크로스이어 작업 원가 이중계상 (리포트 발행됨)
- `비용 관리 > 작업별 비용`(날짜필터)이 두 해 걸친 작업을 **겹치는 각 연도에 전액** 노출(모델 C). 연도합산 시 2배. 연도필터 화면(시작연도)과 불일치. 라이브: 작업별 1년창 72,273,386 − 전년도시작 8건 70,411,221 = 당해 1,862,165.
- **리포트**: `reports/qa-crossyear-cost-report.html` (Artifact 비공개 링크). 권고 **B(시작연도 앵커 귀속)**. datepicker 월/년 nav 무반응(F-2)도 정리.
- ⚠ **Atlassian 미인증**으로 Confluence/Jira 미등재 — 인증 시 등재 가능.

### 3-2. 스코어 출력 설정 기획-구현 차이 5건 (td17)
- 토글 라벨 `미사용/사용`→**`미노출/노출`** · 로고 **[사진등록] 버튼 추가** · 파일규격 표기 **JPEG 누락**('JPG, PNG') · 안내문구 확장 · 저장 활성=변경조건. (score-output 리포트 diff 시트)

---

## 4. 미완/보류 (환경 의존)

- **FILTER 미노출 잔여**: 장비 관제 필터(존재, 렌더레이스)는 **mpd(EQMON-FILTER)에 폴링 미적용**(mpd 무손상 유지 위해). 필요 시 mpd checkMapFilter에 ~16s 폴링 적용하면 해소 예상. 시설 관제는 필터 부재(정당 SKIP).
- **3D 측정값 시각회귀**: 스캐폴드 완성(`course:3d-visual`)이나 **헤드리스 SwiftShader가 EPT 포인트클라우드 완전 로드 불가**(‘Loading Entwine’ 지속) → 측정 미생성. **GPU 환경(헤디드/GPU-CI)에서 `--update-snapshots`로 baseline 생성** 필요. 그 전까진 SKIP.
- **스코어 출력 설정 all-suite 편입**: 미적용(필요 시 STEP 추가). 이웃 신규 메뉴 **마커 인증 조회**도 미학습.
- **그린 제목/보기 데이터 의존**: green-detail이 데이터 있는 팝업 탐색으로 커버하나, 특정 홀 데이터 없으면 정당 SKIP.

---

## 5. 재개 워크플로 (course = td: course-mng-td)
```bash
npm run course:kill          # 남은 Playwright 브라우저 정리
npm run course:auth          # 수동 로그인 → "저장 완료"
npm run course:kill          # auth 헤디드 고아 정리
npm run course:<spec>        # 테스트 1개
```
- ⚠ **1 재인증 = 1 spec 실행**(세션 TTL). 여러 spec 연속 실행 시 2번째부터 openCourseAdmin 실패(가짜 FAIL 아님).
- td17 어드민은 **별도 인증**: `npm run auth`(course:auth와 다름). score-output 등 td17 작업 시.
- 세션 가드: `course-budget-cost-verify`는 loadedScreens=0(세션만료)면 **기존 리포트 보존**하고 throw(덮어쓰기 방지).

---

## 6. 다음 후보 (우선순위)
1. **크로스이어 QA 리포트 Confluence/Jira 등재** (Atlassian 인증 후) — 제품팀 실제 공유.
2. **스코어 출력 설정 all-suite 편입** + 이웃 신규 메뉴 **마커 인증 조회** 학습(td17).
3. **mpd EQMON-FILTER 폴링 적용**(장비 관제 필터 렌더레이스) — 격리 스펙과 동일 ~16s 폴링.
4. **3D 시각회귀 baseline**: GPU 고정 러너 확보 시 `course:3d-visual --update-snapshots`.
5. **파괴/write-path 코스관리 확장** 등 심화(별도).

---
*세션 메모리 갱신: green-popup-tabs · score-output-setting · crossyear-cost-doublecount · session-one-run-per-login.*
