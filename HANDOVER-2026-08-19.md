# HANDOVER 2026-08-19 — td17 확장(표준적용)·코스관리 폼 E2E 완결·이해관계자 리포트

> SMARTSCORE 경기관제 어드민(td17) + 코스관리(course-mng-td) UI 검증 자동화. 다음 세션 인수인계 안내서.

## 0. 한 줄 요약
① **자동화 검증 표준 v1**(13층위·5축) 정립 → td17에 적용해 **L6 폼상호작용 확장·L10 API 부적합 규명·L2 문구baseline·L14 단언민감도 확대**(P1~P5). ② **코스관리 폼 E2E 16화면 전수 완결**(정보관리 9 + 타대메뉴 6 + 드론 1, 결함 0) + 폼 진입점 5형태 감지 배터리 확립. ③ **리포트를 이해관계자용 일상언어 톤으로 재설계**(커버리지·결과 2종) + 표준 메모리화.

---

## 1. 완료 항목

### A. td17 경기관제
| 항목 | 결과 | 파일 |
|------|------|------|
| 스코어 출력 설정 all-suite 편입 | runRoundMgmt STEP8 + IA_TREE + MENU_LIST | `lib/suites.ts`·`lib/langCheck.ts` |
| 마커 인증 조회 학습(JIRA 근거) | 프로브·locator 완료 · suite 미작성(라이브 대기) | `Admin/_probe-marker-auth.spec.ts`·`locators/admin-marker-auth.md` |
| **자동화 검증 표준 v1** | 13층위·5축·원칙·체크리스트 | `docs/automation-verification-standard.md` |
| **td17 확장 검토** | 폭 완비·깊이 불균형 · 최대갭 L6·L10 | `reports/td17-expansion-review.html` |
| P1 L6 폼 배터리 이식 | 프로브로 코스배터리 **비매핑** 확인→등록모달 FILL(PASS 6/0) | `Admin/form-fill-e2e.spec.ts`·`Admin/_probe-form-controls.spec.ts` |
| P2 L10 API 계약 | **thick-client N/A 확정**(reload도 0건) + 폰트 가짜FAIL 제거 | `lib/apiContract.ts`(ASSET_URL)·`Admin/api-contract.spec.ts` |
| P3 L2 문구 전문 baseline | 43화면 캡처 **✅커밋(abc51cf)** | `Admin/notice-baseline.spec.ts`·`baselines/notice-text.td17.json` |
| P4 L12 시각회귀 확대 | 6→22화면(baseline은 CI 러너) | `Admin/visual-regression.spec.ts` |
| P5 L14 단언민감도 확대 | 4→11화면(라이브 민감16/둔감15) | `Admin/assertion-sensitivity.spec.ts` |

### B. 코스관리 (course-mng-td)
| 항목 | 결과 | 파일 |
|------|------|------|
| 코스 기본 정보 심화 E2E | 사진팝업·수정폼 PASS 10/0 | `Course/course-info-basic-e2e.spec.ts` + probe 2 |
| **공용 폼 배터리** | 폼 5형태 감지 + 배터리6종 + datepicker 달력위젯 | `lib/course/formE2E.ts` |
| 정보 관리 10종 폼 E2E | 폼 8종 커버 PASS 36/0/19 | `Course/course-info-e2e-all.spec.ts` |
| 타 대메뉴 폼 E2E | 지도 제외 25화면 PASS 26/0/36 | `Course/course-forms-e2e-all.spec.ts` |
| 거래처/관리기준·시설총괄 진입점 규명 | 5형태 감지 확장(모달·페이지·취소·라우트·인라인) | `Course/course-info-vn-ev-probe`·`course-forms-probe`·`course-facility-reg-probe` |
| 지도 격리 하네스 | 드론사진 업로드 PASS 5/0/2 | `Course/course-map-form-e2e.spec.ts`(MAP_TARGET env) |
| **코스 리뉴얼 정보 심화** | 탭이동·라디오·신규등록/수정 양경로 PASS 21/0/0 | `Course/course-renewal-e2e.spec.ts` |
| → **폼 16화면 전수 E2E 완결**(결함 0) | | |

### C. 리포팅
| 항목 | 결과 | 파일/URL |
|------|------|------|
| monocart-reporter 코스 설정 | 이후 코스 실행 시 자동 생성 | `Course/playwright.config.ts` → `Course/monocart-report/` |
| 커버리지 대시보드(내부용) | 트리맵·트렌드·분기91.5%·뮤테이션 | `reports/course-coverage-dashboard.html` (artifact fc40e9c4) |
| **이해관계자용 커버리지 리포트** | 메뉴별 기능·깊이 · 실측 157항목 | artifact 9d14a0b7 · `reports/course-status-report.html` |
| **이해관계자용 결과 리포트** | 통과율99.5%·결함4→0·추이·신뢰도·진행단계 | artifact 216504eb · `reports/course-result-report.html` |

---

## 2. ★ 핵심 교훈 (재사용)

1. **세션 = 재인증 1회당 1런** (td17·course 공통, 공유 QA 계정). 라이브 실행 전 **`auth/.auth/*.json` mtime 확인 필수** — 재인증해도 파일 미갱신 시 로그인 페이지로 빠짐(수동 로그인 완주 필요). 코스는 `course:kill → course:auth → 로그인 완주 → course:kill`.
2. **층위/배터리 이식 = 복사 아님, 프로브로 재스코프**. 실증: ①코스 배터리가 td17에 비매핑(clear-X 없음·datepicker 달력전용·토글 즉시반영) → 등록모달 FILL로 재설계 ②td17 SPA는 thick-client → API 계약 검증 부적합(N/A). 착수 전엔 안 보임.
3. **폼 진입점 5형태 감지**(`openEditableForm`): 모달·페이지(저장)·페이지(취소판정)·**라우트 이동(/reg)**·인라인(저장상주). 비표준 라벨(거래처 등록·시설 등록·저장하기)은 프로브 규명 후 정규식 확장. 라우트 폼은 **goBack으로 폐기**.
4. **비동기(Vue) 2-phase**: 클릭→nextTick 대기→재조회. 동기 read는 오판(클리어 0/N). 임계 완화(≥1=PASS)로 혼합필드(select/datepicker/이미지 .fill() 비대상) false-FAIL 제거.
5. **라우트 폼→goBack 후 상세/버튼 미복원** → 여러 폼 경로를 한 세션서 검증 시 **경로 사이 `gotoCourseMenu` 재진입**으로 트리거 노출 보장(리뉴얼 EDIT·REG).
6. **리포트는 이해관계자 톤**(신규 표준 [[report-standard]]): 일상언어·실측수치·통과율≠신뢰도·미확인≠갭·청중분리(대외 plain vs 내부 기술). 앞으로 모든 리포트에 적용.

---

## 3. 미완 / 보류
- **마커 인증 조회(td17)**: 프로브·locator 완료, `runMarkerAuth` suite·spec 미작성 → 라이브(`npm run auth`) 프로브 실행→`analysis/라운드관리_마커_인증_조회.json` 확정 후 작성 + runRoundMgmt STEP9 편입.
- **P4 L12 시각 baseline**: 22화면 코드 완료, baseline은 **고정 CI 러너서 `--update-snapshots`** 생성 필요(로컬↔CI 폰트 오탐).
- **P1 후속(td17)**: 메시지 관리·계정 리스트 등록 트리거/구조 상이로 SKIP → 프로브로 확장 가능.
- **코스 뮤테이션 실측**: 단언민감도 스펙 코스 이식 + Stryker mutate를 `lib/course/domain`으로 확장(현재 td17 도메인 대상) → 코스 kill 점수.
- **monocart 리치화**: 코스 스펙이 단일 test라 monocart는 test 단위 표시 → `test.step()`으로 화면별 트리화 시 세부 결과 노출(재실행 필요).

---

## 4. ⚠ 미커밋 상태 (중요)
- **미커밋 62건** — 이번 세션 신규 스펙·배터리·프로브·리포트·표준문서·config 다수. 커밋된 것은 **notice baseline(abc51cf)뿐**.
- 주요 미커밋: `lib/course/formE2E.ts` · `Course/course-{info-basic-e2e,info-e2e-all,forms-e2e-all,renewal-e2e,map-form-e2e}.spec.ts` + probe 5종 · `Admin/{form-fill-e2e,visual-regression(수정),assertion-sensitivity(수정),api-contract(수정),score-output}.spec.ts` · `lib/{suites,langCheck,apiContract}.ts` 수정 · `docs/automation-verification-standard.md` · `reports/*.html` 리포트 4종 · `Course/playwright.config.ts`(monocart) · `package.json` 스크립트 다수.
- ⚠ `.git/index.lock` **stale 락 재발 가능**(이번에 22:01 잔재 제거함). 커밋 전 확인.
- **권고**: 논리 단위로 나눠 커밋(예: 코스 폼E2E / td17 P1~P5 / 리포트 / 표준문서). main 브랜치 워크플로 확인.

---

## 5. 재개 워크플로
```bash
# td17 경기관제
npm run auth                     # 수동 로그인 완주 → admin.json 갱신 확인(mtime)
npm run test:all                 # 전체테스트 / 또는 개별 spec

# 코스관리 (별도 인증)
npm run course:kill
npm run course:auth              # 수동 로그인 완주 → course.json 갱신 확인
npm run course:kill
npm run course:info-e2e-all      # 정보관리 폼 E2E / renewal-e2e / forms-e2e-all 등
```
- ⚠ **1 재인증 = 1 spec 실행**. 여러 spec은 재인증 반복.
- 신규 스크립트: `test:notice`·`test:sensitivity`·`test:visual`(td17) / `course:renewal-e2e`·`course:map-form-e2e`·`course:forms-e2e-all`·`course:info-e2e-all`·각종 probe(course).

## 6. 다음 후보 (우선순위)
1. **미커밋 62건 정리 커밋**(논리 단위) — 최우선(산출물 유실 방지, `.git/index.lock` 확인).
2. **마커 인증 조회 suite 작성**(td17, 라이브 프로브 후).
3. **P3 문구 baseline 회귀 가동**: `npm run test:notice`로 문구 변경 자동 감지(baseline 커밋됨).
4. **코스 뮤테이션 실측** + monocart test.step() 리치화.
5. **이해관계자 리포트 톤을 td17에도 적용**([[report-standard]]).

---
*세션 메모리 갱신: automation-design-standard · td17-verification-expansion · course-info-basic-coverage · report-standard · score-output-setting · marker-auth-inquiry.*
