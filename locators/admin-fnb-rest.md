# 식음 관리 잔여 — 식당 관리 / 상품 등록 관리 / 주문 내역 관리 (리뉴얼 td17)

> 분석일: 2026-06-09 (`analysis/식음_관리_*.json` + probe 기준) / 클럽: 킹즈락
> IA 스코프: 식음 관리 하위 — 드라이브 상세 TC 미작성 → **구조 기반 검증** 예정
> ⚠️ 헤더 GNB = `관제 어드민`. 어드민 도달 판정은 URL(`/club/`)+SNB(`.depth-1-title`).

---

## 🔴 핵심 발견: 식음 관리 실 SNB = **4종** (IA 변경표 6종 ≠ 구현)
실 SNB depth-2(2026-06-09 추출) 기준 식음 관리 하위는 다음 **4개만 존재**:
| 실 SNB 메뉴 | URL | 상태 |
|------|------|------|
| 버전 및 설정 | `/club/page/table-order-version` | ✅ 검증 완료(`runFnbVersion`) |
| 식당 관리 | `/club/page/table-order-restaurant` | 본 문서 ① |
| 상품 등록 관리 | `/club/page/table-order-product` | 본 문서 ② |
| 주문 내역 관리 | `/club/page/table-order-statistics` | 본 문서 ③ |

**미구현(SNB 부재) 2종** — IA 변경표엔 있으나 실 SNB에 없음 → `runIA`에서 **미구현**으로 기록됨:
- **그늘집 및 TOS관리** (SNB 항목 없음 → 진입 시 home으로 폴백)
- **식당·품목 매핑** (SNB 항목 없음) — IA 변경표 '신규(✚)'였으나 미구현

> 📌 IA 변경표 기준 `IA_TREE`는 의도적으로 6종 유지(runIA가 구현/미구현 판정). 단 검증 스위트는 구현된 3종만 작성.

---

## ① 식당 관리 (`/club/page/table-order-restaurant`) — 마스터-디테일
| 영역 | Locator | 비고 |
|------|---------|------|
| 안내문구 | `.info-box-text` | "운영 중인 식당을 생성 및 관리할 수 있습니다. 식당을 선택하여 판매할 메뉴를 관리할 수 있습니다." (2줄) |
| 좌: 식당 리스트 박스 | `.contents-box` filter `식당 관리` | 제목 `식당 관리` + [+ 식당 추가] |
| [+ 식당 추가] | `getByRole('button',{name:'+ 식당 추가'})` 또는 `/식당\s*추가/` | 🔴 생성 — 노출만 검증, 클릭 금지 |
| 아이콘 범례 | `그늘집 식당 아이콘 설명` 박스 | 3종: 아이콘없음(테이블오더만)/캐디앱만/테이블오더+캐디앱 |
| 식당 행 | 리스트 행(번호 + 식당명 + 아이콘 + [관리]) | **≥1**(킹즈락 8개: 대식당/스타트/객실대식당/옥식당2/옥식당4/ttete/옥식당5/흠...). 데이터 의존 |
| 행 [관리] | 행 컨테이너 스코프 `getByRole('button',{name:'관리'})` | 선택 시 우측에 메뉴 관리 노출. 🔴 비파괴(클릭은 조회성이나 편집 진입 → 노출만 검증 권장) |
| 우: 디테일 | `.contents-box` "리스트에서 식당을 선택해 주세요." | 초기 빈 안내. 식당 선택 시 메뉴 관리 영역 |

> 검증 포인트(RESTO-0x 예정): 안내문구 / [+ 식당 추가] 노출 / 아이콘 범례 3종 / 식당 행 ≥1 + [관리] / 우측 초기 안내.
> ⚠️ 비파괴: 식당 추가·관리(편집)·삭제 동작 금지. 우측 디테일은 행 클릭 필요 → 데이터 의존, 노출만 검증.

---

## ② 상품 등록 관리 (`/club/page/table-order-product`) — 검색 + 상품 테이블
| 영역 | Locator | 비고 |
|------|---------|------|
| 안내문구 | `.info-box-text` | "식당에서 판매할 메뉴의 등록 및 관리를 할 수 있습니다. 메뉴별 이미지, 가격 등을 설정할 수 있습니다." (2줄) |
| 상품명 검색 | `getByPlaceholder('상품명을 입력하세요.')` + [검색] | 조회(읽기) — 비파괴 |
| [검색] | `getByRole('button',{name:'검색'})` | |
| [선택삭제] | `getByRole('button',{name:'선택삭제'})` | 🔴 삭제 — 노출만(초기 비활성) |
| [원산지 관리] | `getByRole('button',{name:'원산지 관리'})` | 🔴 모달 진입 — 노출만 |
| [카테고리 관리] | `getByRole('button',{name:'카테고리 관리'})` | 🔴 모달 진입 — 노출만 |
| [상품 등록] | `getByRole('button',{name:'상품 등록'})` | 🔴 생성 — 노출만 |
| 상품 테이블 헤더 | columnheader | **No / 썸네일 / 카테고리 / 상품명 / 가격 / 조리시간(분) / 운영시간 / 노출** (8컬럼) |
| 행 select 체크박스 | `tbody input[type=checkbox]` | 선택삭제용. 헤더 전체선택 1 + 행별 (≥0) |
| 노출 상태 | 행 `노출` 뱃지/토글 | 데이터 의존. 🔴 토글 변경 금지 |
| 상품 행 | `tbody tr` | **≥1**(킹즈락 38행). 데이터 의존, 고정 count 금지 |

> 검증 포인트(FNBPROD-0x 예정): 안내문구 / 상품명 검색 input+[검색] / 액션 4버튼(선택삭제·원산지·카테고리·상품 등록) 노출 / 테이블 8헤더 / 행 ≥1.
> ⚠️ 비파괴: 등록·삭제·카테고리/원산지 관리·노출 토글 변경 금지. 검색은 조회성(클릭 가능하나 노출 검증으로 갈음 권장).

---

## ③ 주문 내역 관리 (`/club/page/table-order-statistics`) — 통계 대시보드
> IA 비고: **캐디주문실적·그늘집주문내역 통합**. 우상단 탭으로 두 관점 전환.

| 영역 | Locator | 비고 |
|------|---------|------|
| 안내문구 | `.info-box-text` | "주문내역을 관리할 수 있습니다. 식당 별, 기간 별 주문 현황에 대하여 확인 및 엑셀 다운로드가 가능합니다." (2줄) |
| 관점 탭 | 우상단 버튼 `캐디주문실적` / `그늘집주문내역` | 2종 통합 뷰 전환 |
| 기간 datepicker | `.datepicker-input` ph `YYYY.MM.DD` ×2 | 시작~종료. ⚠️ 달력 전용(fill 미반영) → 노출만 |
| 기간 단위 | 버튼 `일별`/`주별`/`월별` | |
| 식당 필터 | `.v-select` (`전체 식당`) | vue-select |
| 캐디 필터 | `.v-select` (`전체 캐디`) | vue-select |
| [검색]/[초기화] | `getByRole('button',{name:'검색'\|'초기화'})` | 조회성 — 비파괴 |
| 안내(기간 제한) | `기간 선택은 최대 12개월 이내로 설정하세요…` | |
| 요약 카드 4 | `.contents-box` (카드) | 총 주문금액 / 총 주문건수 / 평균 주문금액 / 주문TOP캐디. 값 데이터 의존(0 가능) |
| 차트 | 주문금액 추이(테이블 날짜/주문금액/주문건수) / 캐디별 주문금액 Top 5 / 식당 주문 비중 | SVG 차트 |
| 주문 랭킹 테이블 | columnheader | **순위/캐디명/캐디별명/식당/주문건수/공급가/부가세/주문금액/평균주문금액** (9컬럼). 빈 시 "데이터가 없습니다." |
| 주문 상세 내역 테이블 | columnheader | **No/캐디명/식당/주문내역/주문건수/주문금액/주문일시 ▼** (7컬럼) + 총계 표기 |
| [내보내기] | 주문 상세 박스 `getByRole('button',{name:'내보내기'})` | 다운로드 — `runCommonActions`로 처리 가능 |

> 검증 포인트(FNBORD-0x 예정): 안내문구 / 관점탭 2종 / 기간 datepicker×2 / 일·주·월 버튼 / 식당·캐디 필터 / 검색·초기화 / 요약카드 4 / 주문 랭킹 9헤더 / 주문 상세 7헤더 / 내보내기.
> ⚠️ 비파괴: 조회/필터/내보내기는 읽기성. 값·행수는 데이터 의존(0/빈상태 허용, 고정 count 금지). 기간은 노출만.

---

## 공통 비파괴 / 주의
- SNB/2뎁스 토글(`snb-toggle__switchbtn`, `depth-2-toggle__switchbtn*`)은 네비 chrome — 검증 대상 아님.
- 모든 생성/수정/삭제/토글은 **노출·활성만** 검증. 조회/검색/내보내기만 실행 허용(비파괴).
- 데이터(식당·상품·주문 행)는 골프장 의존 → `≥1`/빈상태 허용.
- `gotoMenu` 진입 시 자동 RAW 스캔 적용.

---

## ✅ 구현 완료 (2026-06-09)
- `lib/suites.ts`: `runFnbRestaurant`(RESTO-01~05) / `runFnbProduct`(FNBPROD-01~05) / `runFnbOrderHistory`(FNBORD-01~09) 추가.
- `Admin/fnb.spec.ts`(식음 4종 통합: 버전 및 설정 포함) + `all-suite.spec.ts` STEPS 등록.
- 실행: **PASS 27/27**(첫 메뉴 진입 플레이크는 재실행 통과). 리포트 `reports/fnb_report_*.xlsx`.
- 미구현 2종(그늘집 및 TOS관리·식당·품목 매핑)은 `runIA`가 미구현 기록(IA_TREE 6종 유지).
