# 태블릿 운영 관리 > 메시지 관리 / 홀 이벤트 관리 (리뉴얼 td17) - Locator

> 분석일: 2026-06-04 (`analysis/태블릿_운영_관리_메시지_관리.json`, `..._홀_이벤트_관리.json`)
> 헤더 GNB = `관제 어드민`. 구조 기반 검증(드라이브 상세 TC 미작성). 안내문구는 **부분 일치**(전문 일치 추후).
> ⚠️ 비파괴: 저장/입력란 추가/적용/수정/삭제/홀이벤트·아이템 추가/드래그&드롭 = 노출만 검증

---

## A. 메시지 관리 (`/club/page/live-message`) — `runTabletMessage` (TMSG-01~07)
| 영역 | Locator | 비고 |
|------|---------|------|
| 안내문구 | `.info-box-text` | "태블릿 및 센터에서 자주 사용하는 메시지를 등록 후 순서를 배정…" (부분 검증) |
| 탭 5종 | `.tab-group` 내 텍스트 | 태블릿 메시지 / 셀프모드 메시지 / 뒷카트 알림 메시지 / 센터 메시지 / 분실물&요청물품 |
| 버튼 배열 미리보기 | `.tablet-grid` + `저장` | 드래그&드롭 순서 조정(비파괴) |
| 메시지 관리 | `입력란 추가`/`적용` + table(제목/메시지/관리) | 행별 `수정`/`삭제`(비파괴) |
| 예약어 힌트 | `.msg-hint-box` | "[메시지 예약어] 홀인원:!HIO …" |
| 버전 | `현재버젼 N ▲n` 버튼 | 표기용 |

## B. 홀 이벤트 관리 (`/club/page/live-hole-event`) — `runTabletHoleEvent` (THEV-01~05)
| 영역 | Locator | 비고 |
|------|---------|------|
| 안내문구 | `.info-box-text` | "특정 홀에 노출될 이미지… 700 X 480 사이즈" 🔴 **오타 의심('이미지가태')** → 차이 기록 |
| 홀이벤트 섹션 | `.contents-box`(홀이벤트) + vue-select(전체코스/전체홀) + `적용`/`홀이벤트 추가` | 필터 비파괴 |
| 홀이벤트 테이블 | 컬럼 코스/홀 번호/홀이벤트/이미지/이벤트 노출시간/관리 (rowCount 데이터 의존) | 행별 `수정`/`삭제`(비파괴) |
| 아이템 섹션 | `아이템 추가` + `.pre…`아이템명 input(ph "아이템명을 입력하세요.") + `.item-card`(≥1) | 카드별 `수정`/`삭제`(비파괴) |
| 버전 | `현재버전 N ▲n` 버튼 | |

> 데이터 의존(메시지 9행·홀이벤트 5행·아이템 4카드)은 `≥1`로 검증. 탭/필터는 조회용(클릭 안 함, 노출만).
